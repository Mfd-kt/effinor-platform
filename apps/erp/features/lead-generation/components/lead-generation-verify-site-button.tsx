"use client";

type Props = {
  stockId: string;
  disabled?: boolean;
  disabledReason?: string;
};

// TODO: Reimplement when a new public-site verification source replaces Firecrawl.
export function LeadGenerationVerifySiteButton(_props: Props) {
  return null;
}
