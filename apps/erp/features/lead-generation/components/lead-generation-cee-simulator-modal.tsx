"use client";

// TODO: Le simulateur CEE (cee-workflows) a été retiré. Ce composant est désactivé
// jusqu'à la mise en place d'un nouveau flux de conversion lead-generation → prospect CRM.
export function LeadGenerationCeeSimulatorModal(_props: {
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
  stock?: unknown;
  sheets?: unknown;
  activity?: unknown;
  destratProducts?: unknown;
  onConversionComplete?: () => void;
}) {
  return null;
}
