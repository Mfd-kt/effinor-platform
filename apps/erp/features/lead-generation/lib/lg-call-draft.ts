/** Libellé d’une activité « appel lancé » créée au clic sur Aircall — fusionnée au prochain enregistrement du formulaire. */
export const LG_CALL_STARTED_ACTIVITY_LABEL = "Appel lancé — complétez le compte rendu" as const;

/** Fenêtre pendant laquelle le brouillon d’appel peut être complété par le formulaire (ms). */
export const LG_CALL_STUB_MERGE_MS = 2 * 60 * 60 * 1000;
