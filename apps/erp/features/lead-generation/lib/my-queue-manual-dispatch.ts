/** Lot par défaut pour le bouton « Récupérer des fiches » (Mes fiches à traiter). */
export const MY_QUEUE_MANUAL_CHUNK_DEFAULT = 20;

/** Plafond de fiches actives (assignations en cours) — la récupération manuelle ne peut pas le dépasser. */
export const MY_QUEUE_MAX_ACTIVE_STOCK = 100;
