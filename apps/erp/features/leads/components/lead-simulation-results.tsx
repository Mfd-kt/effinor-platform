// TODO: Le simulateur CEE (cee-workflows + features/leads/simulator) a été retiré.
// Ce composant est désactivé jusqu'à la mise en place du nouveau pipeline simulation.
export function LeadSimulationResults(_props: {
  lead?: unknown;
  workflows?: unknown;
}) {
  return null;
}
