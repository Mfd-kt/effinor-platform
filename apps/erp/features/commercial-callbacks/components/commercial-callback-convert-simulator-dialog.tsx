"use client";

// TODO: Le simulateur CEE (cee-workflows) a été retiré. Ce composant est désactivé
// jusqu'à la mise en place d'un nouveau flux de conversion rappel → lead.
export function CommercialCallbackConvertSimulatorDialog(_props: {
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
  row?: unknown;
  sheets?: unknown;
  destratProducts?: unknown;
  onConverted?: () => void;
}) {
  return null;
}
