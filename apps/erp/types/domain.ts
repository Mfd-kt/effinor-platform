/**
 * Types métier / UI dérivés (Phase 3 — squelette).
 * Les enums métiers stricts viendront de `database.types.ts` une fois généré.
 */

export type ModulePlaceholderProps = {
  title: string;
  description: string;
};
