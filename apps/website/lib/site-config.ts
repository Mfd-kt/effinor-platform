/**
 * @deprecated Import depuis `@/lib/site-config` conservé pour compatibilité.
 * Config statique : `site-config.defaults.ts` — données dynamiques : `site-settings.ts`.
 */
export { siteConfig, type SiteConfig } from './site-config.defaults'
